namespace: site\tests
actor: Tester
paths:
    tests: tests
    log: tests/_log
    data: tests/_data
    helpers: tests/_support
settings:
    bootstrap: _bootstrap.php
    colors: true
    memory_limit: 1024M
    log: true
coverage:
  enabled: true
  whitelist:
    include:
      - models/*
modules:
  config:
    Yii2:
      configFile: 'config/test-local.php'
